import 'package:flutter/material.dart';

class AbsensiUI extends StatelessWidget {
  static const String namePath = '/absensi_page';
  const AbsensiUI({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
